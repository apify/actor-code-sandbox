{
  "actorSpecification": 1,
  "name": "opencode",
  "title": "OpenCode",
  "description": "Instant access to OpenCode - Open-source AI coding assistant running on Apify",
  "version": "0.0",
  "buildTag": "latest",
  "usesStandbyMode": false,
  "meta": {
    "generatedBy": "claude-haiku-4.5"
  },
  "input": {
    "title": "Actor input schema",
    "description": "Just run the Actor and use OpenCode with Apify. The OpenCode window will automatically open in the Actor run console.",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
      "initShellScript": {
        "title": "Initialization Script",
        "type": "string",
        "description": "Optional bash script to run on startup for installing packages or setting up the environment.",
        "editor": "textarea",
        "prefill": "apt-get update\n# apt-get install -y curl"
      },
      "idleTimeoutSeconds": {
        "title": "Idle Timeout Seconds",
        "type": "integer",
        "description": "Automatically shut down after this many seconds of inactivity. Set to 0 to disable. Recommended: set Actor timeout to 0 (infinite) when using this.",
        "default": 600,
        "editor": "number",
        "prefill": 600
      }
    },

    "required": []
  },
  "output": {
    "actorOutputSchemaVersion": 1,

    "title": "OpenCode",
    "description": "",
    "type": "object",

    "properties": {
      "url": {
        "type": "string",
        "title": "Main page",
        "template": "{{run.containerUrl}}/shell?arg=-c&arg=source%20/app/sandbox_bashrc%3B%20opencode"
      }
    }
  },
  "dockerfile": "./Dockerfile"
}
