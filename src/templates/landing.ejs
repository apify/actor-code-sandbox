<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sandbox | Home</title>
    <link rel="icon" type="image/x-icon" href="/favicon.ico">
    <style>
        :root { color-scheme: light dark; }
        * { box-sizing: border-box; margin: 0; padding: 0; }
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
            background: radial-gradient(circle at 18% 20%, #f0f6ff, #e7ecf2 38%, #f8fafc) fixed, #0b1220;
            color: #0f172a;
            padding: 32px;
            min-height: 100vh;
        }
        .page { max-width: 1080px; margin: 0 auto; display: flex; flex-direction: column; gap: 16px; }
        .hero {
            display: flex; justify-content: space-between; align-items: center; gap: 12px;
            padding: 20px 24px; background: #0f172a; color: #e2e8f0; border-radius: 12px;
            box-shadow: 0 20px 40px rgba(15, 23, 42, 0.35);
        }
        .eyebrow { letter-spacing: 0.08em; text-transform: uppercase; font-size: 12px; color: #94a3b8; }
        h1 { font-size: 28px; margin-bottom: 6px; }
        .lead { color: #cbd5e1; }
        .badge { background: #22c55e; color: #0f172a; padding: 8px 12px; border-radius: 999px; font-weight: 600; white-space: nowrap; }
        .card {
            background: rgba(255, 255, 255, 0.9); border: 1px solid #e2e8f0; border-radius: 12px;
            padding: 18px 20px; box-shadow: 0 12px 30px rgba(15, 23, 42, 0.08);
            display: flex; flex-direction: column; gap: 10px; height: 100%;
        }
        .card-header { display: flex; justify-content: space-between; align-items: center; gap: 12px; }
        .actions { display: flex; gap: 10px; flex-wrap: wrap; }
        .button {
            background: linear-gradient(120deg, #2563eb, #22d3ee); color: #0f172a; padding: 10px 14px;
            border-radius: 8px; font-weight: 700; text-decoration: none; box-shadow: 0 10px 20px rgba(34, 211, 238, 0.35);
        }
        .button.secondary { background: linear-gradient(120deg, #22c55e, #a3e635); box-shadow: 0 10px 20px rgba(34, 197, 94, 0.3); }
        .button:hover { filter: brightness(1.05); }
        .muted { color: #475569; margin-top: 8px; }
        .grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(320px, 1fr)); gap: 12px; align-items: stretch; }
        .list { list-style: none; display: grid; gap: 8px; margin-top: 12px; }
        code {
            background: #0f172a; color: #e2e8f0; padding: 6px 8px; border-radius: 6px;
            font-family: "SFMono-Regular", Consolas, "Liberation Mono", Menlo, monospace; display: inline-block; width: 100%; overflow-x: auto;
        }
        .code-block-wrapper { position: relative; margin-top: 8px; }
        .code-block { 
            background: #0f172a; color: #e2e8f0; padding: 14px 16px; border-radius: 8px;
            font-family: "SFMono-Regular", Consolas, "Liberation Mono", Menlo, monospace; font-size: 13px; 
            overflow-x: auto; white-space: pre; line-height: 1.5;
        }
        .copy-btn {
            position: absolute; top: 8px; right: 8px; background: #334155; color: #e2e8f0;
            border: none; padding: 6px 12px; border-radius: 6px; font-size: 12px; font-weight: 600;
            cursor: pointer; transition: all 0.2s;
        }
        .copy-btn:hover { background: #475569; }
        .copy-btn:active { background: #22c55e; color: #0f172a; }
        .copy-btn.copied { background: #22c55e; color: #0f172a; }
        .example-label { font-weight: 600; color: #475569; margin-top: 16px; margin-bottom: 4px; }
        .example-label:first-child { margin-top: 0; }
        @media (max-width: 640px) {
            body { padding: 20px; }
            .hero { flex-direction: column; align-items: flex-start; }
            .badge { align-self: flex-start; }
            .card-header { flex-direction: column; align-items: flex-start; }
        }
    </style>
</head>
<body>
    <main class="page">
        <header class="hero">
            <div>
                <p class="eyebrow">Apify AI Sandbox</p>
                <h1>Landing page</h1>
                <p class="lead">Connect through REST, MCP, or the interactive shell.</p>
            </div>
            <% if (isLocalMode) { %>
                <div class="badge"><%= modeLabel %></div>
            <% } %>
        </header>

        <section class="card">
            <div class="card-header">
                <h2>Quick links</h2>
                <div class="actions">
                    <a class="button" href="/shell/" target="_blank" rel="noopener noreferrer">Open interactive shell</a>
                    <a class="button secondary" href="/health" target="_blank" rel="noopener noreferrer">Check health</a>
                </div>
            </div>
        </section>

        <section class="grid">
            <div class="card">
                <h3>MCP endpoint</h3>
                <p class="muted">POST</p>
                <code><%= serverUrl %>/mcp</code>
            </div>
            <div class="card">
                <h3>Health</h3>
                <p class="muted">GET</p>
                <code><%= serverUrl %>/health</code>
            </div>
            <div class="card">
                <h3>LLMs.txt</h3>
                <p class="muted">GET</p>
                <code><%= serverUrl %>/llms.txt</code>
            </div>
        </section>

        <section class="card">
            <h3>Connect with Claude Code (MCP)</h3>
            <div class="code-block-wrapper">
                <button class="copy-btn" onclick="copyCode(this)">Copy</button>
                <pre class="code-block">claude mcp add --transport http sandbox <%= serverUrl %>/mcp</pre>
            </div>
        </section>

        <section class="card">
            <h3>Code examples</h3>
            
            <p class="example-label">Call the API (TypeScript/Node)</p>
            <div class="code-block-wrapper">
                <button class="copy-btn" onclick="copyCode(this)">Copy</button>
                <pre class="code-block">const baseUrl = '<%= serverUrl %>';
const res = await fetch(`${baseUrl}/exec`, {
    method: 'POST',
    headers: { 'content-type': 'application/json' },
    body: JSON.stringify({ command: 'print("hello")', language: 'py', timeoutSecs: 10 }),
});
const json = await res.json();
console.log(json);</pre>
            </div>

            <p class="example-label">Call the API (Python)</p>
            <div class="code-block-wrapper">
                <button class="copy-btn" onclick="copyCode(this)">Copy</button>
                <pre class="code-block">import requests

base_url = "<%= serverUrl %>"
payload = {"command": "print('hello')", "language": "py", "timeoutSecs": 10}
resp = requests.post(f"{base_url}/exec", json=payload, timeout=15)
resp.raise_for_status()
print(resp.json())</pre>
            </div>
        </section>

        <section class="card">
            <h3>Core REST endpoints</h3>
            <ul class="list">
                <li><code>POST <%= serverUrl %>/exec</code> – run shell commands or code
                    <br><span style="color: #64748b; font-size: 13px;">Shell: { command: "ls -la", cwd: "/sandbox", timeoutSecs: 5 }</span>
                    <br><span style="color: #64748b; font-size: 13px;">Code: { command: "print('hello')", language: "py", timeoutSecs: 10 }</span></li>
            </ul>
        </section>

        <section class="card">
            <h3>Filesystem endpoints (RESTful)</h3>
            <p class="muted" style="margin-bottom: 12px;">Direct file operations using RESTful HTTP methods. All paths relative to /sandbox.</p>
            <ul class="list">
                    <li><code>GET <%= serverUrl %>/fs/{path}</code> – read file or list directory
                        <br><span style="color: #64748b; font-size: 13px;">Example: GET /fs/app/log.txt or GET /fs/app?download=1 (ZIP)</span></li>
                    <li><code>PUT <%= serverUrl %>/fs/{path}</code> – write/replace file (raw body)
                        <br><span style="color: #64748b; font-size: 13px;">Example: PUT /fs/config.json with JSON body</span></li>
                    <li><code>POST <%= serverUrl %>/fs/{path}?mkdir=1</code> – create directory
                        <br><span style="color: #64748b; font-size: 13px;">Example: POST /fs/project/src?mkdir=1</span></li>
                    <li><code>POST <%= serverUrl %>/fs/{path}?append=1</code> – append to file (raw body)
                        <br><span style="color: #64748b; font-size: 13px;">Example: POST /fs/log.txt?append=1 with text body</span></li>
                    <li><code>DELETE <%= serverUrl %>/fs/{path}</code> – delete file or directory
                        <br><span style="color: #64748b; font-size: 13px;">Example: DELETE /fs/temp?recursive=1</span></li>
                    <li><code>HEAD <%= serverUrl %>/fs/{path}</code> – get file metadata (headers only)
                        <br><span style="color: #64748b; font-size: 13px;">Example: HEAD /fs/data.json (returns Content-Type, Size, etc.)</span></li>
            </ul>
        </section>
    </main>

    <script>
        function copyCode(button) {
            const codeBlock = button.nextElementSibling;
            const code = codeBlock.textContent;
            
            navigator.clipboard.writeText(code).then(() => {
                const originalText = button.textContent;
                button.textContent = 'Copied!';
                button.classList.add('copied');
                
                setTimeout(() => {
                    button.textContent = originalText;
                    button.classList.remove('copied');
                }, 2000);
            }).catch(err => {
                console.error('Failed to copy:', err);
            });
        }
    </script>
</body>
</html>